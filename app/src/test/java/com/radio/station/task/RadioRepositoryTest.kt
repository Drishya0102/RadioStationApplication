/*
package com.radio.station.task

import com.radio.station.task.repository.RadioRepository
import kotlinx.coroutines.runBlocking
import org.junit.Assert.assertTrue
import org.junit.Test

class RadioRepositoryTest {
    @Test
    fun `fetchStations returns valid data`() = runBlocking {
        val mockApiService = MockApiService()
        */
/*val repository = RadioRepository(mockApiService)

        val stations = repository.getStations()
        assertTrue(stations.isNotEmpty())*//*

    }
}
*/
